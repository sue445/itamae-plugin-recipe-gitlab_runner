RSpec.describe Itamae::Plugin::Recipe::GitlabRunner do
  it "has a version number" do
    expect(Itamae::Plugin::Recipe::GitlabRunner::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
